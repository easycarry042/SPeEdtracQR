includes:
    - vendor/larastan/larastan/extension.neon
    # Pre-existing Eloquent type-inference findings, parked for incremental
    # burndown. New code is still held to the level below; delete entries here
    # as models/queries get properly typed.
    - phpstan-baseline.neon

parameters:
    level: 5

    paths:
        - app
